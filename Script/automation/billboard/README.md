Billboard automation scripts live here.

Kept entrypoints:

- `run_billboard_isolated.py`: starts editor with automation control and runs isolated billboard smoke.
- `test_billboard_isolated.py`: RPC-driven isolated billboard regression assertions.

Older ad hoc capture scripts were exploratory and should not stay at `Script/` root.

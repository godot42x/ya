# GUI 框架 / 引擎模块化拆分计划

> 状态：2026-08-08 建立。阶段性工件，不代表当前默认主工作流。

## 边界

- 记录"把 YA Engine 拆成可独立链接的模块集，其中 GUI 框架可作为独立库被
  宿主消费"这条重构线的目标、决策、步骤与进度。
- 本计划以 RHI 命名渲染抽象层（`render` → `rhi`），并明确 RDG 不属于 RHI。
- 完成并沉淀为稳定规则后，迁入 skill；历史坑迁入 memory。

## 读取时机

- 需要理解引擎模块边界、GUI 框架抽取、build 目标拆分上下文时。
- 需要查本专项的 baseline、验证命令或已提交改动时。

## 相关 plan

- `../game-ui-rendering/`：Node2D 统一 scene tree 的 UI 功能线（并行推进）。

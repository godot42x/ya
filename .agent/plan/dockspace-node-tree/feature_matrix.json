{
  "plan": "dockspace-node-tree",
  "updated_at": "2026-08-19",
  "legend": {
    "planned": "尚未开始，但已纳入主线",
    "in_progress": "当前阶段正在推进",
    "pass": "已有结构证据或已完成",
    "fail": "已有明确回归或验证失败",
    "blocked": "受前置审计、外部条件或上游决定阻塞"
  },
  "tracks": [
    {
      "id": "dock_node_tree",
      "title": "DockSpace → ImGui 风格 DockNode 嵌套停靠树",
      "items": [
        {
          "id": "n0_drag_lifecycle",
          "status": "pass",
          "notes": "已增加通用 DragSessionObserver 与 EDragFinishResult；覆盖每次 move、target-change、Dropped/NoTarget/Cancelled，保持 string payload 与旧 beginDrag/onDrop 兼容。widgets focused tests、GUIWorkbench 构建与 headless dock baseline 均通过。"
        },
        {
          "id": "n1_dock_model",
          "status": "planned",
          "notes": "新建纯 DockNode model：unique_ptr 二叉树、stable NodeId/PanelId、panel registry、transaction 与 invariant validator；FDockNode 不持有 UITabBar/UIContainer/UIElement 指针"
        },
        {
          "id": "n2_projection_layout",
          "status": "planned",
          "notes": "Dock model -> WidgetTree projection：Split 用 UISplitPane，Leaf view 用 tab bar+content；divider ratio 回写 model；删除 _groups[3] / rebuildZone"
        },
        {
          "id": "n3_cardinal_preview_drop",
          "status": "planned",
          "notes": "center + 上下左右的 resolveDropPlan、preview、transaction；resize/release 时重新解析 plan，禁止依赖旧 hover cache"
        },
        {
          "id": "n4_corner_compound",
          "status": "planned",
          "notes": "四 corner 的 two-split compound plan + visible persistent empty leaf；extent 不足必须 disabled，不能视觉/行为降级"
        },
        {
          "id": "n5_floating_panel",
          "status": "planned",
          "notes": "Popup layer 普通 DockFloatingHost + 多实例 FloatingDockWindow；NoTarget release tear-off、re-dock、z-order 与 focus restore，不复用 UIPopupOverlay"
        },
        {
          "id": "n6_port_persistence_boundary",
          "status": "planned",
          "notes": "定义 model DTO 与 IDockWorkspaceCoordinator 调用方向；不实现磁盘 persistence 或多 OS window"
        },
        {
          "id": "n7_scenario_coverage",
          "status": "planned",
          "notes": "model tests + headless dumps + window/GPU tests：id uniqueness、geometry、route/focus、8 zone、floating/modal、teardown"
        }
      ]
    }
  ]
}

{
  "plan": "gui-capability-gap",
  "updated_at": "2026-08-18",
  "legend": {
    "planned": "尚未开始，但已纳入主线",
    "in_progress": "当前阶段正在推进",
    "pass": "已有结构证据或已完成",
    "fail": "已有明确回归或验证失败",
    "blocked": "受前置审计、外部条件或上游决定阻塞"
  },
  "tracks": [
    {
      "id": "reactive_binding",
      "title": "响应式数据绑定 + 性能管线（第一阶段，已收口）",
      "items": [
        {
          "id": "m0_perf_baseline",
          "status": "pass",
          "notes": "GuiPerfStats（layout/paint 耗时 + paintedWidgets + drawItems）已随 buildSnapshot 填充；closure test 115/115"
        },
        {
          "id": "m1_reactive_core",
          "status": "pass",
          "notes": "Reactive<T> 事件驱动依赖追踪 + 双缓冲 draw-item 段复用 + rebuiltWidgets 计数；UIText.bindText；双向析构清理。closure 118/118"
        },
        {
          "id": "m2_binding_generalize",
          "status": "pass",
          "notes": "EDirtyLevel（Paint/Layout 两级）+ Button.bindEnabled + SplitPane.bindSplitRatio + ReactiveList + Computed 接口。closure 121/121"
        },
        {
          "id": "m3_perf_seam",
          "status": "pass",
          "notes": "buildSnapshot 桥接 profiling::metrics()（gui.tree.* key）；声明式 diff 口子内嵌（bindCache/cacheItems/reuseCachedItems）。closure 122/122"
        },
        {
          "id": "styles_styleset",
          "status": "pass",
          "notes": "FWidgetStyle + UIStyleSet + UIText.bindStyle；closure 123/123"
        },
        {
          "id": "treeview",
          "status": "pass",
          "notes": "UITreeView：ReactiveList 数据源 + 展开/选中；paint 展平 + 箭头按钮 hover。closure 126/126"
        },
        {
          "id": "gui_workbench_gallery",
          "status": "pass",
          "notes": "Gallery 页覆盖 widget/reactive/style；TextField 滚动/TreeView clip/attach 顺序等修复已收口；modal scenario 更新"
        }
      ]
    },
    {
      "id": "editor_parity",
      "title": "第二阶段：GameEditor ImGui 替换前置（GUI App 线全量补齐）",
      "items": [
        {
          "id": "p1_vector_primitives",
          "status": "pass",
          "notes": "UIFrameDrawItem::EKind 增 Line（lineFrom/lineTo/lineThickness）；UIFrameBuilder 增 addLine/addRectOutline/addBezierCubic（bezier 客户端细分）；Render2DComposePass Line 分支=旋转细 quad（makeSprite transform 版，零管线改动）；Gallery section 4 + gallery_vector.jsonl 通过；modal 回归通过"
        },
        {
          "id": "p2_table_grid",
          "status": "pass",
          "notes": "UITableLayout/UITableSlot（列宽 0=stretch 均分/行高/padding）+ UITableGrid 数据驱动控件（bindData ReactiveList<FTableRow> + bindSelection Reactive<int>，行选中/hover/列分隔线走 P1 addLine）；WidgetTreeDump 加 tableGrid control 块；Gallery section 5 + gallery_table.jsonl 通过；vector 回归通过"
        },
        {
          "id": "p3_input_controls",
          "status": "pass",
          "notes": "InputExtras.h/.cpp 五控件：UIDragFloat（capture 拖拽+键盘步进）、UISpinBox（+/- 区）、UIRadioButton（组由 host 管理）、UIColorEdit（色块点击循环通道+拖动调值）、UISearchComboBox（焦点过滤+UIMenu 弹出）；WidgetTreeDump 五 control 块；Gallery section 6 + gallery_inputs.jsonl 通过；vector/table 回归通过"
        },
        {
          "id": "p4_drag_reorder",
          "status": "pass",
          "notes": "UIDragSource（press+6px 阈值起 beginDrag）+ UIDropTarget（_accept 谓词 + _onDrop + VisualFlag 高亮 + paint 用 P1 addRectOutline 画高亮框）；WidgetTreeDump dragSource/dropTarget 块；Gallery section 7 + gallery_drop.jsonl 通过；vector/table/inputs/dragdrop_interaction 全回归通过"
        },
        {
          "id": "ga_paint_self_clip",
          "status": "pass",
          "notes": "UIElement::paint 模板默认 pushClip(_layoutRect)（_bSelfClip opt-out）；TreeView/TableGrid/TextField 手写 clip 移除验证护栏生效；全 7 场景回归通过"
        },
        {
          "id": "ga_styleset_set_semantics",
          "status": "pass",
          "notes": "UIStyleSet::define 同名复用 handle + set（绑定者自动 notify）；全回归通过"
        },
        {
          "id": "gb_event_timestamp",
          "status": "pass",
          "notes": "Event 基类构造自动打 steady_clock 时间戳（inline 时钟读，跨 DLL 安全）；场景驱动每步 +10ms 模拟时间（确定性双击）；DragFloat/SpinBox 双击改 400ms 时间窗判定；acceptance 9 checkpoint 通过"
        },
        {
          "id": "gb_debug_validation_frame",
          "status": "pass",
          "notes": "debug 构建每 60 帧 unbound builder 全量重画 diff 增量（draw item 逐项比对）；验证：正常代码 2537 次校验 0 误报；人为漏标脏（canvas 不稳定 paint）182 次告警精确命中 item；probe 已撤销"
        },
        {
          "id": "p5_treeview_editing",
          "status": "pass",
          "notes": "TreeView 三能力叠加：_bReorderable（press 6px 阈值起 drag，payload tree-node:，drop 行 1/3 判定 before/into/after，_onReorder 回调 host 重建数据，P1 高亮线/框）+ _onContextMenu（右键回调 host 开菜单）+ bindFilter（Reactive<string>，匹配链过滤+过滤时全展开）；UITableGrid 升级 cell 任意 widget（UITableLayout 布局 child + cellHasWidget 抑制文本）；Gallery TreeView demo 带 reorder/右键/过滤框 + Table cell 按钮；gallery_tree_edit.jsonl（visibleRows 4 过滤断言）通过；全回归通过"
        },
        {
          "id": "gc_scenario_render",
          "status": "planned",
          "notes": "--scenario-render 开关（frame 步骤真实渲染）+ WidgetTree 校验 mismatch 计数 + 场景 assert_validation_clean 断言；gallery_acceptance 渲染变体验证零告警；人为漏标脏注入验证断言失败"
        },
        {
          "id": "p6_interaction_p1",
          "status": "planned",
          "notes": "tooltip（UIElement::setTooltip）/ TextWrapped（UIText _bWrap）/ 子树禁用（_bEnabled）/ UIDialog 模态；Gallery section 9 + gallery_p1.jsonl"
        },
        {
          "id": "p7_dockspace",
          "status": "planned",
          "notes": "UIDockSpace + UIDockLayout（中央区+边缘停靠+tab 合并+分割条）；独立 Dock 页 + dock.jsonl；smoke tab 索引同步更新"
        }
      ]
    }
  ]
}
struct DenseMap<T> {
    keys: Vec<usize>,
    values: Vec<T>,
}

struct SparseSet {
    keys: Vec<usize>,
}

// struct Registry {
//     components: std::collections::HashMap<TypeId, ComponentStorage>,
//     id: u32,
// }
